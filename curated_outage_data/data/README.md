# Local Data

This folder is for local curated-data inputs and generated artifacts.

```text
data/raw/        raw source downloads
data/interim/    temporary join outputs and normalized source extracts
data/processed/  reproducible curated artifacts
```

These folders are gitignored. Keep source notes, schemas, and validation
summaries in git; keep raw and generated data local unless we explicitly decide
to publish a small sample.
