# Handoff — 2026-06-23 → next session

## 60-second summary
1. The redesigned **Next.js dashboard is LIVE**: https://outage-pricing-wsd6lcl64q-uc.a.run.app/dashboard (replaced the old python static dashboard; old revision retained for rollback).
2. Two-audience product: outward **Pricing** + internal **Underwriting Studio** (tabs: Price Breakdown · Baseline · County Clustering · Adjusters) + **Settings** (global ER/TM loadings + data source).
3. **Baseline** tab is the **Trust & Posture cell read** (per `cell_read_fundamentals.md`): TRUST = weakest-link(coverage·sample·stability); POSTURE = cushion LEVEL by duration + TILT vs peers. Two orthogonal axes, never merged. Built from the (gitignored) cell-read CSV, now wired into `build_data.py`.
4. **County Clustering** tab: regime identity + cross-T strip + annual ≥8h series w/ LS-trend overlay + peer mix grouped as **4 behavioral regimes + a 2-faced abstention**.
5. **Regime label fix:** `insufficient` was overloaded — `recent-change` (125 counties, data-rich) now surfaces as **"Recent change"**, sparse subs as **"Insufficient data."** Communication fix only — classifier/schema unchanged. Docs updated to match (methodology §3 + cluster README).
6. **Settings** = platform-wide loadings (applied everywhere); the Studio panel = **manual loads** (the legit per-county lever); location/forward are model outputs, not sliders.
7. Engine is pure `composePremium()`; per-county data bundle is `web/lib/data/*.json` regenerated by `web/scripts/build_data.py` (run it after any data change).
8. **Deploy = push to `deploy/outage-pricing`** → GHA Docker-builds `web/`, deploys, routes traffic. No manual gcloud/dispatch needed (a personal account can't flip traffic; the GHA SA can).
9. **basePath `/dashboard`** is baked at build + runtime; raw `fetch`/`<img>` must go through `api()`/`asset()` in `web/lib/base-path.ts` or they 404.
10. **Only Adjusters remains** — and it's a **quantitative workstream, not UI** (promote/validate Location shadow; build a real Forward signal). The tab honestly shows them as developing today.

## Repro / verify current state
```
# local dev
cd web && npm run dev            # → localhost:3000  (basePath unset locally)
# rebuild data bundle
.venv/bin/python3 web/scripts/build_data.py
# verify live
curl https://outage-pricing-wsd6lcl64q-uc.a.run.app/dashboard            # 200 · <title>Pricing</title>
# deploy: just push to deploy/outage-pricing (GHA does the rest); watch:
gh run watch $(gh run list --workflow=deploy-outage-pricing.yml --branch deploy/outage-pricing --limit 1 --json databaseId --jq '.[0].databaseId') --exit-status
```

## Files to read before continuing
- **Spec of record:** `docs/plan/dashboard_redesign/02_studio_section_spec.md` (per-tab plots, data-maturity, build sequence)
- **Trust & posture:** `docs/methodology/02_per_customer/cell_read_fundamentals.md`
- **Regime taxonomy:** `docs/methodology/03_risk_clustering/{regime_classification_methodology.md, README.md}` (the recent-change surfacing rule is in §3 + the README note)
- **Adjusters source material:** `docs/extra/location_features/`, `docs/dicsscssion/location_aware_outage_pricing/`, `curated_outage_data/schemas/county_lambda_shadow.md`; Forward: `docs/OUTAGE_MODELING_FRAMEWORK.md` (Step 5), `docs/plan/05_forward_regime/forward_looking_modeling_plan.md`
- **Code:** `web/components/studio/tabs/*.tsx`, `web/components/studio/shared.ts`, `web/scripts/build_data.py`, `web/lib/{quote-store,base-path}.tsx`, `web/Dockerfile`, `.github/workflows/deploy-outage-pricing.yml`
- **Memory:** `project_studio_spec.md`, `project_dashboard_redesign.md` (distilled index of all the above)

## NEXT ACTION — Adjusters (the one remaining Studio piece)
Adjusters has two lanes; both are data/quant work, not UI:

**Lane A — Location (within-county relativity) · status: SHADOW → promote + validate**
- Artifacts exist at `price_engine/dashboard/data/{density_relativity,county_location_basis,tract_density.json}` and `density_spearman_by_county.csv` — but they are **NOT in `web/lib/data/`** and not in the quoted price.
- Validated only on PoUS CT/MA/RI (one quiet season). Known flaw: population density mis-ranks dense commercial cores (Midtown Manhattan reads "rural") → fix is zonal NLCD impervious.
- Steps: (1) promote artifacts into the web bundle via `build_data.py`; (2) render the within-county relativity (tercile bars, dispersion, the CONUS map, Spearman strip) **clearly labeled shadow / not-in-price**; (3) the activation gate is validation outside CT/MA/RI + a full storm season — do NOT wire it into the quoted premium until then.

**Lane B — Forward (climate/grid) · status: FRAMED PLACEHOLDER (no signal exists)**
- No field in any JSON, no code in `build_data.py`. Blocked on: a cause-tagged event dataset (cause coverage ~24%, below the 80% gate), curated grid features (EIA-861 not yet in `model_features.parquet`), and a rolling-origin backtest that beats the flat baseline.
- The Step-3 regime is a **router** into Step-5 forward methods (regime → which forward machinery is allowed), and that bridge is unbuilt. Render Forward as honest placeholders + the Step3→5 routing diagram. **Never ship Forward as a number** until the governance gate (external validation) is met.

**Guardrails that must hold (from the build):**
- The regime is a router/diagnostic — it **never moves the price**.
- Posture/location/forward shown honestly with active/modeled/placeholder status; don't manufacture depth.
- Keep docs↔dashboard↔notebook consistent on any taxonomy/label change (we just had to reconcile all three).
- After any data change: rerun `build_data.py`, strict-validate the JSON (no NaN/Inf), `npm run build`, then push.

## Known issues
- None blocking. Adjusters is intentionally a placeholder (its quant groundwork is the next workstream).
- Access constraints to remember: personal gcloud can't flip Cloud Run traffic (lacks AR download); gh token can't `workflow_dispatch` (no repo-admin) → deploy only via push.
